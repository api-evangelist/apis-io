{
  "tools": [
    {
      "name": "apis_io_search",
      "description": "START HERE. Federated overview of the APIs.io catalog for a query: the top matching APIs, providers, AND tags in one call, each with its total match count. Filter by tags, artifact types, industry, region, rating band. `limit` is per section. To page/sort the full list of ONE type, use find_apis / find_providers / find_tags; for one entity, get_api / get_provider.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "return": {
            "type": "string",
            "enum": [
              "apis",
              "providers",
              "tags"
            ],
            "description": "Return only this kind of match. Default: every kind."
          },
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "artifact_types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "band": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Rating bands: exemplar, strong, developing, thin, minimal."
          },
          "min_score": {
            "type": "number"
          },
          "industry": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "region": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "area": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "composite",
              "name",
              "created",
              "modified",
              "api_count",
              "demand"
            ],
            "description": "Order results. Default: relevance with a query, composite (quality) when browsing. `demand` = Fortune-1000 adoption, scoped to the query."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 25,
            "default": 5,
            "description": "Top matches to return per section (apis/providers/tags)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_playground_apis",
      "description": "Curated APIs that are SAFE TO EXPERIMENT WITH \u2014 no key (or an instant, free, self-serve key), no cost, and no real-world side effects. Free, and deliberately so: this is where you start if you are new here. Every entry carries an `example_request` URL that returned HTTP 200 to an unauthenticated GET on the date shown, so it can be called immediately with no setup. Use this when you are new to this catalog, learning what an API call feels like, testing a tool-use loop, or need a harmless endpoint to demonstrate something. Defaults to the `starter` set; pass grouping=\"deep-archive\" for institutional/OAI-PMH repositories.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "access": {
            "enum": [
              "no-key",
              "instant-key"
            ],
            "description": "no-key = callable right now; instant-key = free self-serve key."
          },
          "grouping": {
            "enum": [
              "starter",
              "deep-archive"
            ],
            "description": "starter (default) is the good-first-experience set."
          },
          "tag": {
            "type": "string",
            "description": "Filter by subject tag, e.g. weather, books, energy."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 50,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_apis",
      "description": "Paginated, sortable list of APIs across providers \u2014 filter by tag, provider, artifact type, industry, region, or rating band. Use apis_io_search first for a cross-type overview.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "resolve": {
            "type": "string",
            "enum": [
              "on",
              "off"
            ],
            "default": "on",
            "description": "When `q` exactly names a tag, apis.io uses it: on PROVIDERS a resource tag (order, webhook, image) replaces the text search with the companies that classify themselves there \u2014 2,182 text matches for \"order\" become the 92 that claim it; on APIS the tag's members are ADDED to the text matches, never swapped for them. Market tags (payments) and tags almost no company claims are reported and not applied. Set `off` for pure text. The resolution is always echoed as `resolved_tag`."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "artifact_types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "band": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Rating bands: exemplar, strong, developing, thin, minimal."
          },
          "min_score": {
            "type": "number"
          },
          "industry": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "region": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "area": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "pricing": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Access model \u2014 pricing: free, freemium, free-trial, paid, enterprise. (providers only)"
          },
          "onboarding": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Access model \u2014 how to start: open (no key), self-serve, approval. (providers only)"
          },
          "try_now": {
            "type": "boolean",
            "description": "Only providers a developer can start using at no cost right now (free/trial + self-serve/open). (providers only)"
          },
          "public": {
            "type": "boolean",
            "description": "Only providers callable publicly with no signup (onboarding=open). (providers only)"
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "composite",
              "name",
              "created",
              "modified",
              "api_count",
              "demand"
            ],
            "description": "Order results. Default: relevance with a query, composite (quality) when browsing. `demand` = Fortune-1000 adoption, scoped to the query."
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Return exactly these top-level keys (overrides view)."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_api",
      "description": "Full detail for one API by aid (provider:api-slug); set include=[\"content\"] to inline artifact bodies. Results carry `next`: the sub-resources that exist for this entity and the exact tool call that retrieves each, computed from this record. Pass include_next=false to omit it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "aid"
        ],
        "properties": {
          "aid": {
            "type": "string"
          },
          "artifact_types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "include_next": {
            "type": "boolean",
            "default": true,
            "description": "Set false to omit the `next` affordance block."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_providers",
      "description": "Paginated, sortable list of providers \u2014 filter by text, tag, artifact type, industry, region, rating band, access model (pricing/onboarding/try_now/public), or DIRECTION (accepts_writes/min_acting \u2014 whether the surface takes a write or only emits). `tags` filters on what the COMPANY does (its own top-level classification), not on what its endpoints touch; for a resource-level term use find_apis?tags=, or api_tags= here to find companies whose APIs merely touch it. Use apis_io_search first for a cross-type overview.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "resolve": {
            "type": "string",
            "enum": [
              "on",
              "off"
            ],
            "default": "on",
            "description": "When `q` exactly names a tag, apis.io uses it: on PROVIDERS a resource tag (order, webhook, image) replaces the text search with the companies that classify themselves there \u2014 2,182 text matches for \"order\" become the 92 that claim it; on APIS the tag's members are ADDED to the text matches, never swapped for them. Market tags (payments) and tags almost no company claims are reported and not applied. Set `off` for pure text. The resolution is always echoed as `resolved_tag`."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "artifact_types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "band": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Rating bands: exemplar, strong, developing, thin, minimal."
          },
          "min_score": {
            "type": "number"
          },
          "industry": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "region": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "area": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "pricing": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Access model \u2014 pricing: free, freemium, free-trial, paid, enterprise. (providers only)"
          },
          "onboarding": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Access model \u2014 how to start: open (no key), self-serve, approval. (providers only)"
          },
          "try_now": {
            "type": "boolean",
            "description": "Only providers a developer can start using at no cost right now (free/trial + self-serve/open). (providers only)"
          },
          "public": {
            "type": "boolean",
            "description": "Only providers callable publicly with no signup (onboarding=open). (providers only)"
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "composite",
              "name",
              "created",
              "modified",
              "api_count",
              "demand"
            ],
            "description": "Order results. Default: relevance with a query, composite (quality) when browsing. `demand` = Fortune-1000 adoption, scoped to the query."
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Return exactly these top-level keys (overrides view)."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "api_tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Providers that publish an API, spec or OPERATION carrying these tags, without claiming it as their business. The complement of `tags`, and disjoint from it."
          },
          "accepts_writes": {
            "type": "boolean",
            "description": "true = the provider has operations that ACT (create/update/send), so you can push something to it; false = measured and read-only. Use this when the question is \"where can I publish/send/post to\" \u2014 text search cannot tell direction, and without it a query about syndicating content returns RSS feeds and read-only APIs. Providers with no agentic-access reading are excluded either way: absent is unmeasured, not zero."
          },
          "min_acting": {
            "type": "integer",
            "description": "Minimum number of acting (non-read) operations. Use to require real write depth rather than a single write endpoint."
          },
          "country": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Providers whose derived country membership includes these slugs (e.g. united-kingdom). Membership is partial \u2014 a count is a floor."
          },
          "max_score": {
            "type": "number",
            "description": "Only providers at or below this composite. Pair with min_score for a band-free range."
          },
          "trend": {
            "type": "string",
            "description": "up | down | flat \u2014 direction of the composite since the previous scoring pass."
          },
          "facet": {
            "type": "string",
            "description": "Quality facet to threshold on: discoverability, contract_quality, governance, operational_transparency, developer_ergonomics, commercial_clarity. Needs min_facet."
          },
          "min_facet": {
            "type": "number",
            "description": "Minimum score on `facet`. Without `facet` this does nothing."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider",
      "description": "Full detail for one provider: profile, rating, and the APIs it publishes. The heavy `collections` and `common` sections are omitted by default (their counts are still reported) \u2014 pass view=full for the whole document, or fields=[\"common\"] for a section. Narrow for cheap reads: fields=[\"score\"] returns just the rating. Relative artifact URLs are resolved to absolute, fetchable ones. Results carry `next`: the sub-resources that exist for this entity and the exact tool call that retrieves each, computed from this record. Pass include_next=false to omit it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Return exactly these top-level keys (slug and name always included); overrides view."
          },
          "include_next": {
            "type": "boolean",
            "default": true,
            "description": "Set false to omit the `next` affordance block."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_tags",
      "description": "Paginated, sortable list of tags in the taxonomy. Filter by `axis` to separate MARKETS (a business someone is in) from CAPABILITIES (the name of a resource in somebody's OpenAPI) \u2014 the single most useful distinction in the vocabulary, and a coin flip without it. A tag can carry more than one axis, so axis=market means market is ONE of its axes. Use apis_io_search first for a cross-type overview.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "composite",
              "name",
              "created",
              "modified",
              "api_count",
              "demand"
            ],
            "description": "Order results. Default: relevance with a query, composite (quality) when browsing. `demand` = Fortune-1000 adoption, scoped to the query."
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Return exactly these top-level keys (overrides view)."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "axis": {
            "type": "string",
            "description": "market | capability | persona | posture. Comma-separated for more than one."
          },
          "band": {
            "type": "string",
            "description": "canonical | established | emerging | niche | stale | posture."
          },
          "min_api_count": {
            "type": "integer",
            "minimum": 0,
            "description": "Only tags applied to at least this many APIs."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_tag",
      "description": "One tag: its metadata, neighbor tags, and a top sample of its providers and APIs (with totals). `provider_count` counts companies that classify THEMSELVES here; `api_provider_count` counts the ones reached only through an API or operation tag \u2014 the two are disjoint, and a tag with no providers and many APIs names a resource, not a market. Use find_apis?tags=slug / find_providers?tags=slug for the full ranked list, or view=full here. Results carry `next`: the sub-resources that exist for this entity and the exact tool call that retrieves each, computed from this record. Pass include_next=false to omit it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25,
            "description": "Top members to show per list."
          },
          "include_next": {
            "type": "boolean",
            "default": true,
            "description": "Set false to omit the `next` affordance block."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_industries",
      "description": "Browse industry verticals; sort by provider/API count.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string"
          },
          "sort": {
            "type": "string"
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_industry",
      "description": "One industry: metadata + a top sample of its member providers (ranked, with the total). Use find_providers?industry=slug for the full list, or view=full here. Results carry `next`: the sub-resources that exist for this entity and the exact tool call that retrieves each, computed from this record. Pass include_next=false to omit it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25,
            "description": "Top members to show."
          },
          "include_next": {
            "type": "boolean",
            "default": true,
            "description": "Set false to omit the `next` affordance block."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_regions",
      "description": "Browse geographic regions; sort by provider/API count.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string"
          },
          "sort": {
            "type": "string"
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_region",
      "description": "One region: metadata + a top sample of its member providers (ranked, with the total). Use find_providers?region=slug for the full list, or view=full here. Results carry `next`: the sub-resources that exist for this entity and the exact tool call that retrieves each, computed from this record. Pass include_next=false to omit it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25,
            "description": "Top members to show."
          },
          "include_next": {
            "type": "boolean",
            "default": true,
            "description": "Set false to omit the `next` affordance block."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_countries",
      "description": "Browse the country dimension \u2014 the finest of the four place taxonomies, and the one a buyer usually names (\"who is in Germany\"). Peer to find_industries / find_regions / find_areas. Search by text; sort by provider count. Membership is derived and known to be partial: read a count as a floor, not a census.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string"
          },
          "sort": {
            "type": "string"
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_country",
      "description": "One country: metadata, counts, and a top sample of member providers (with the total). Use find_providers?country=slug for the full ranked list, or view=full here.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25,
            "description": "Top members to show."
          },
          "include_next": {
            "type": "boolean",
            "default": true,
            "description": "Set false to omit the `next` affordance block."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_areas",
      "description": "Browse the API Evangelist areas taxonomy (authentication, webhooks, payments, \u2026) \u2014 curated, scored provider indexes. Search by text; sort by provider count.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string"
          },
          "sort": {
            "type": "string"
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_area",
      "description": "One area: metadata, its dedicated site link, and a top sample of member providers (with the total). Use find_providers?area=slug for the full list, or view=full here. Results carry `next`: the sub-resources that exist for this entity and the exact tool call that retrieves each, computed from this record. Pass include_next=false to omit it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25,
            "description": "Top members to show."
          },
          "include_next": {
            "type": "boolean",
            "default": true,
            "description": "Set false to omit the `next` affordance block."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_extensions",
      "description": "Browse every OpenAPI `x-` specification extension harvested across the catalog, with the provenance the OpenAPI Initiative registry does not carry: who publishes it, in how many documents, where in the document it appears, and what shape the value takes. Search by key or purpose; sort by occurrences.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string"
          },
          "sort": {
            "type": "string"
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_extension",
      "description": "One OpenAPI specification extension: its publishers, occurrence and document counts, where it appears, and the value shapes observed. CHECK is_api_evangelist BEFORE treating a key as provider-originated \u2014 some of these are published by API Evangelist, not by a vendor.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "include_next": {
            "type": "boolean",
            "default": true,
            "description": "Set false to omit the `next` affordance block."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_ratings",
      "description": "Ranked ratings leaderboard \u2014 filter by band, score range, trend, or facet threshold. Sort by composite (default), delta (biggest gainers), or any quality facet (governance, security, \u2026) to rank providers by that dimension.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "band": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "min_score": {
            "type": "number"
          },
          "trend": {
            "type": "string"
          },
          "facet": {
            "type": "string"
          },
          "min_facet": {
            "type": "number"
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "max_score": {
            "type": "number",
            "description": "Only ratings at or below this composite. Pair with min_score for a range."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Restrict to these provider slugs \u2014 the ratings of a named set."
          },
          "sort": {
            "type": "string",
            "enum": [
              "composite",
              "delta",
              "discoverability",
              "contract_quality",
              "governance",
              "operational_transparency",
              "developer_ergonomics",
              "commercial_clarity"
            ],
            "description": "Leaderboard order; default composite."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_rating",
      "description": "One provider's full rating breakdown (composite, band, trend, six facets).",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_cohorts",
      "description": "START HERE for market-level questions. Every scored population of providers in the catalog \u2014 tags, industries, regions, areas and VC portfolios \u2014 as one browsable index. Filter by kind, minimum population, or promotion tier (report/brief/data). Then call cohort_stats for the distribution, cohort_rankings for the leaderboard.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tag",
              "industry",
              "region",
              "area",
              "vc"
            ],
            "description": "Restrict to one roster source."
          },
          "min_providers": {
            "type": "integer",
            "description": "Only cohorts with at least this many providers. 25+ is brief-grade, 100+ report-grade."
          },
          "tier": {
            "type": "string",
            "enum": [
              "report",
              "brief",
              "data"
            ],
            "description": "Promotion tier: report (100+, report-grade), brief (25+), data (everything else)."
          },
          "q": {
            "type": "string",
            "description": "Substring match on cohort name or slug."
          },
          "page": {
            "type": "integer"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_cohort",
      "description": "One cohort: its identity, its population, and the full member roster with each provider's Kin Score and agent readiness. Understanding plan. The analysis parts (stats/rankings/scores/capabilities) need Influence.",
      "inputSchema": {
        "type": "object",
        "required": [
          "kind",
          "slug"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tag",
              "industry",
              "region",
              "area",
              "vc"
            ]
          },
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_tag_groups",
      "description": "START HERE for capability-map questions. The 1,500+ tag groups the catalog computes \u2014 sets of tags that demonstrably travel together across providers (Cards, Logistics, Wealth Management, Observability), the layer between a single tag and a coarse facet. Every response carries a `coverage` block: about a quarter of the vocabulary is in NO group, and any count taken from here should be read against `ungrouped_share`. Pass exclude=cross_cutting,sparse to drop the groups that are plumbing rather than markets.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Substring match on group name, slug or member tag."
          },
          "basis": {
            "type": "string",
            "enum": [
              "cooccurrence",
              "facet"
            ],
            "description": "cooccurrence groups are evidence-based; facet groups are a weaker fallback bucket for tags with no co-occurrence edge."
          },
          "exclude": {
            "type": "string",
            "description": "Comma-separated: cross_cutting (plumbing every provider has), sparse (a chain label propagation strung together, not a cluster). Use both when drawing a capabilities map."
          },
          "min_tags": {
            "type": "integer",
            "description": "Only groups holding at least this many tags."
          },
          "page": {
            "type": "integer"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_tag_group",
      "description": "One tag group: its member tags and the full provider roster resolved from the catalog, each with Kin Score and agent readiness. Filter the roster by industry, region, country or area. Free; the DISTRIBUTION over the roster (`stats`) needs Understanding. Note `source_provider_count` vs `provider_count` \u2014 the first comes from an index that caps band membership at 1,000, the second is resolved live and is the complete number.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string",
            "description": "Tag group slug, e.g. cards, logistics, wealth-management."
          },
          "industry": {
            "type": "string",
            "description": "Narrow the roster to one or more industry slugs (comma-separated)."
          },
          "region": {
            "type": "string",
            "description": "Narrow the roster to one or more region slugs (comma-separated)."
          },
          "country": {
            "type": "string",
            "description": "Narrow the roster to one or more country slugs (comma-separated)."
          },
          "area": {
            "type": "string",
            "description": "Narrow the roster to one or more area slugs (comma-separated)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "tag_group_tags",
      "description": "The member tags of one tag group, each joined to its catalog record \u2014 provider count, api count, tag quality band and composite. Use it to see what a group is actually made of before trusting the group as a market.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "page": {
            "type": "integer"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "cohort_stats",
      "description": "UNDERSTANDING \u2014 the DISTRIBUTION for a whole market: Kin Score mean/median/min/max/stdev, agent-readiness spread, band split, per-facet averages, and artifact adoption rates. This is the market-stats sheet from a Trend Report, computed live. Always carries a `coverage` block saying how much of the roster is actually scored AND how deeply we enriched it (`enrichment_depth`: mean catalog_gap and median artifact directories). Read that before quoting the mean anywhere: cohort enrichment depth spans ~42 points of catalog_gap across the catalog, so part of any cohort number is our coverage rather than the market.",
      "inputSchema": {
        "type": "object",
        "required": [
          "kind",
          "slug"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tag",
              "industry",
              "region",
              "area",
              "vc"
            ]
          },
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "cohort_rankings",
      "description": "UNDERSTANDING \u2014 the leaderboard for a market, on TWO axes: rank by Kin Score composite AND rank by agent readiness. The two orders differ, and the gap between a provider's two ranks is usually the story.",
      "inputSchema": {
        "type": "object",
        "required": [
          "kind",
          "slug"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tag",
              "industry",
              "region",
              "area",
              "vc"
            ]
          },
          "slug": {
            "type": "string"
          },
          "page": {
            "type": "integer"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "cohort_scores",
      "description": "UNDERSTANDING \u2014 facet-level scores for every member of a cohort, each with the cohort average and the delta against it. A 60 in governance means nothing until you know the market sits at 45; this is the endpoint that says so.",
      "inputSchema": {
        "type": "object",
        "required": [
          "kind",
          "slug"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tag",
              "industry",
              "region",
              "area",
              "vc"
            ]
          },
          "slug": {
            "type": "string"
          },
          "page": {
            "type": "integer"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_operations",
      "description": "UNDERSTANDING \u2014 CATALOG-WIDE operation search: which providers expose a path matching your terms. \"Who has a POST /refunds\" had no answer before this \u2014 tags are what a company says about itself, a path is what its API accepts. Terms match path SEGMENTS (all must appear), so q=\"payment intents\" finds /payments/v1/intents. Add method to narrow by verb, deprecated=true for the retiring ones. One row per real operation: a provider's spec is split per tag, so the same call can live in several of its documents, and `apis` lists them all. Reads matching providers' surfaces up to max_providers (cap 100) and tells you in `meta.query.truncated` when more matched than were read.",
      "inputSchema": {
        "type": "object",
        "required": [
          "q"
        ],
        "properties": {
          "q": {
            "type": "string",
            "description": "Path words, all of which must appear, e.g. \"refunds\" or \"payment intents\"."
          },
          "method": {
            "type": "string",
            "enum": [
              "GET",
              "POST",
              "PUT",
              "PATCH",
              "DELETE",
              "HEAD",
              "OPTIONS"
            ]
          },
          "deprecated": {
            "type": "boolean",
            "description": "Only operations the provider marked deprecated."
          },
          "max_providers": {
            "type": "integer",
            "description": "How many matching providers to read (default 40, cap 100)."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "deprecated_operations",
      "description": "UNDERSTANDING \u2014 every provider publishing at least one operation marked deprecated in its own OpenAPI, ranked by how many. The lifecycle view of the catalog: 8,297 deprecated operations across 681 providers, which nothing could see before, because a provider's own deprecated filter needs you to already suspect that provider. Drill in with get_provider_operations(deprecated=true).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "cohort_history",
      "description": "UNDERSTANDING \u2014 the SET-LEVEL time series: how a whole market's Kin Score and agent readiness moved, build by build. There was a per-provider series and a whole-dataset licence and nothing between them, so \"how has banking moved over six months\" had no answer. Returns the MEAN over members present on each date plus members_scored beside it \u2014 membership changes as the catalog grows, and a move you cannot attribute to scores rather than population is not a finding.",
      "inputSchema": {
        "type": "object",
        "required": [
          "kind",
          "slug"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tag",
              "industry",
              "region",
              "area",
              "vc"
            ]
          },
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "cohort_failures",
      "description": "UNDERSTANDING \u2014 what a whole market gets WRONG: the agent-readiness checks its members most commonly fail, ranked by what fixing each would move the cohort mean. The inverse of gap_analysis, which says where a market is under-served; this says where it is failing the rubric. Turns \"fix your OpenAPI\" into \"this industry does not publish OpenAPI\", which is the shape of a finding worth publishing. Named checks only \u2014 facet rollups cannot name the checks underneath them.",
      "inputSchema": {
        "type": "object",
        "required": [
          "kind",
          "slug"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tag",
              "industry",
              "region",
              "area",
              "vc"
            ]
          },
          "slug": {
            "type": "string"
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "cohort_capabilities",
      "description": "UNDERSTANDING \u2014 what every member of a cohort actually publishes: artifact counts by type plus the agent-readiness dimensions each one satisfies. The capability matrix behind a market report. Large cohorts come back in byte-budgeted chunks: while `complete` is false, call again with `cursor` set to `next_cursor`; the last page has `complete: true` and a null `next_cursor`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "kind",
          "slug"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "tag",
              "industry",
              "region",
              "area",
              "vc"
            ]
          },
          "slug": {
            "type": "string"
          },
          "cursor": {
            "type": "integer",
            "minimum": 0,
            "description": "Resume offset from a previous response's next_cursor. Omit for the first page; keep calling while next_cursor is not null."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "compare_cohorts",
      "description": "UNDERSTANDING \u2014 two markets side by side on one rubric: \"is US payments further along than UK banking?\" Returns both distributions plus the deltas on score, agent readiness, every facet and every adoption rate. A question a written report cannot answer, because a report only ever covers one market. CHECK `depth_confounded` FIRST: when the higher-scoring cohort is also the one we enriched more deeply, the delta reflects our own coverage as much as the markets, and `depth_note` says by how much.",
      "inputSchema": {
        "type": "object",
        "required": [
          "a",
          "b"
        ],
        "properties": {
          "a": {
            "type": "string",
            "description": "First cohort as <kind>:<slug>, e.g. \"tag:payments\"."
          },
          "b": {
            "type": "string",
            "description": "Second cohort as <kind>:<slug>, e.g. \"industry:banking\"."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_operations",
      "description": "Every operation a provider exposes, across all of its OpenAPIs \u2014 method, path, operationId, summary, which API it belongs to, and whether it is deprecated. The shortcut for \"what can I actually call here?\", which otherwise means fetching and parsing every one of their specs. Where the provider publishes agentic-access, each operation also carries its action class and CONSEQUENCE (read vs something that moves money) \u2014 filter on those to find the safe surface before letting an agent loose on it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "method": {
            "type": "string",
            "enum": [
              "GET",
              "POST",
              "PUT",
              "PATCH",
              "DELETE",
              "HEAD",
              "OPTIONS"
            ],
            "description": "Restrict to one HTTP method."
          },
          "api": {
            "type": "string",
            "description": "Restrict to one API, by aid or api slug."
          },
          "path": {
            "type": "string",
            "description": "Substring match on the operation path."
          },
          "consequence": {
            "type": "string",
            "description": "Agentic consequence, e.g. \"read\". Only matches operations with agentic-access profiled."
          },
          "action_class": {
            "type": "string",
            "description": "Agentic action class, e.g. \"connected\"."
          },
          "q": {
            "type": "string",
            "description": "Substring match on summary, path, or operationId."
          },
          "page": {
            "type": "integer"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_tools",
      "description": "Every MCP tool a provider ships, with the operation each one wraps and its auth. Reports provenance per tool: `first-party` means the provider publishes that server, `derived` means API Evangelist generated a candidate tool list from their OpenAPI because no hosted server was found. Filter with provenance=first-party for a true picture \u2014 counting derived tools as the provider's own overstates MCP adoption badly. A server whose tools could not be enumerated (auth-gated) is reported in coverage.not_enumerable rather than silently counted as zero.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "provenance": {
            "type": "string",
            "enum": [
              "first-party",
              "derived"
            ],
            "description": "Restrict by who authored the tool list."
          },
          "page": {
            "type": "integer"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_schema",
      "description": "Every JSON Schema a provider publishes \u2014 the data shapes their API works in, in one list, without walking their artifacts. Free.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "q": {
            "type": "string",
            "description": "Substring match on schema name or slug."
          },
          "page": {
            "type": "integer"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_facets",
      "description": "UNDERSTANDING \u2014 one provider's score broken into its facets (discoverability, contract quality, governance, operational transparency, developer ergonomics, commercial clarity) plus any regulatory regime that applies. The composite is one number; these are the parts a team can actually act on.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_agent_readiness",
      "description": "EXPLORE \u2014 the agent-readiness dimensions and standalone score for ONE provider (spec presence, MCP server, auth clarity, idempotency, error semantics, rate-limit signal, well-known catalog, consent identity, dry-run\u2026). A STANDALONE score, not a slice of the composite. For the same question across the catalog \u2014 who is agent-ready, which dimensions have actually diffused \u2014 use find_agent_readiness (Influence).",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_agent_readiness",
      "description": "UNDERSTANDING \u2014 the agent-readiness leaderboard across the whole catalog. Rank providers by agent readiness, and filter to those that DO satisfy (has) or do NOT satisfy (missing) specific dimensions \u2014 e.g. has=\"mcp_server,protected_resource_metadata\" is the OAuth-capable MCP cohort, missing=\"agent_card\" is the addressable market for a fix. Returns agent score + band alongside the Kin Score.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "has": {
            "type": "string",
            "description": "Comma-separated dimensions the provider MUST satisfy (ANDed), e.g. mcp_server,idempotency."
          },
          "missing": {
            "type": "string",
            "description": "Comma-separated dimensions the provider must NOT satisfy (ANDed) \u2014 the gap view."
          },
          "band": {
            "type": "string",
            "description": "Restrict to one or more agent-readiness bands."
          },
          "min_score": {
            "type": "number",
            "description": "Minimum agent-readiness score, 0-100."
          },
          "sort": {
            "type": "string",
            "enum": [
              "agent_score"
            ],
            "description": "Ranked by agent readiness, highest first."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "agent_readiness_dimensions",
      "description": "UNDERSTANDING \u2014 diffusion: for each agent-readiness dimension, how many scored providers satisfy it, out of how many, as a share. The denominator every \"the agent web is/is not here yet\" claim needs and almost never has.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_capabilities",
      "description": "START HERE for \"what can a business do with these APIs\". The business-capability model \u2014 333 top-level capabilities across 28 industries \u2014 with how many catalog providers reach each one. Defaults to capabilities the catalog actually reaches; pass all=true for the whole model INCLUDING the ones with no coverage, which is a real answer rather than a gap: most of what an enterprise does runs on internal systems that publish no API. Free.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Match against name, BC id or industry."
          },
          "industry": {
            "type": "string",
            "description": "e.g. \"Banking & Capital Markets\", \"Software & Technology\"."
          },
          "all": {
            "type": "boolean",
            "description": "Include capabilities with zero catalog coverage (default false)."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_capability",
      "description": "One business capability: its definition, its sub-capabilities, the external authorities it cites, and WHICH catalog providers reach it with their ratings. Accepts a slug or a BC id (e.g. \"payments-card-management\" or \"BC-1340\"). Free. The per-tag edges WITH their evidence are get_capability_edges (Understanding).",
      "inputSchema": {
        "type": "object",
        "required": [
          "capability"
        ],
        "properties": {
          "capability": {
            "type": "string",
            "description": "Slug or BC id."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_capability_edges",
      "description": "UNDERSTANDING \u2014 the evidence behind a capability. Every edge that lands on it: the provider, the tag, the exact OpenAPI file, a calibrated confidence, and the QUOTED FRAGMENT OF THE PROVIDER'S OWN CONTRACT that justifies the claim. Only edges at confidence >= 0.7 whose evidence was found verbatim in the source contract are published, so an edge here is checkable rather than asserted. This is what you cite when someone asks \"says who?\". Understanding plan.",
      "inputSchema": {
        "type": "object",
        "required": [
          "capability"
        ],
        "properties": {
          "capability": {
            "type": "string",
            "description": "Slug or BC id."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_business_capabilities",
      "description": "UNDERSTANDING \u2014 what ONE vendor's published APIs let a business DO, rolled up by capability, with the tag-level evidence under each. Answers \"what is this vendor actually for\" from its own contracts rather than its marketing. NOT get_provider_capabilities, which is artifact counts and agent-readiness dimensions and is free. Understanding plan.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_capabilities",
      "description": "What one provider publishes, counted by artifact type, with the agent dimensions it satisfies. Free \u2014 it is a fact about their own public surface. NOTE: this is artifact counts, NOT business capabilities \u2014 for what the vendor lets a business DO, call get_provider_business_capabilities.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_evidence",
      "description": "How a provider's score was established, part by part: first-party (they published it), verified (we fetched and confirmed it), or derived (we inferred it). Free \u2014 the basis for a claim should never sit behind the claim. Call this before disputing or quoting a score.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_rating_rubric",
      "description": "UNDERSTANDING \u2014 the rubric: bands, facet weights, trend thresholds, so an agent can interpret any score. Moved behind the paywall with the ratings data on 2026-08-31.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_artifacts",
      "description": "Find artifacts of ANY one type across the catalog \u2014 the cross-type entry point. Each type also has a dedicated find_<type> tool (find_mcp, find_rules, find_scopes, \u2026) with sorts and filters unique to that type; prefer those when you know the type. Types: mcp, skills, rules, arazzo, plans, rate-limits, finops, collections, asyncapis, graphql, json-ld, channels, examples, json-schemas, json-structures, openapis, postman, apis-json. PRO: scopes, security. include=[\"content\"] inlines bodies.",
      "inputSchema": {
        "type": "object",
        "required": [
          "type"
        ],
        "properties": {
          "type": {
            "type": "string"
          },
          "q": {
            "type": "string"
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "compare_providers",
      "description": "UNDERSTANDING \u2014 Compare 2+ providers side by side: composite/band, per-facet leaders, and an artifact-coverage matrix.",
      "inputSchema": {
        "type": "object",
        "required": [
          "providers"
        ],
        "properties": {
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Provider slugs to compare (2+)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "gap_analysis",
      "description": "UNDERSTANDING \u2014 For each provider: high-value artifacts it lacks (MCP, Arazzo, Rules, Skills\u2026), what its peers commonly publish but it lacks, and its score vs the peer median \u2014 plus stack-level gaps across the set.",
      "inputSchema": {
        "type": "object",
        "required": [
          "providers"
        ],
        "properties": {
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Provider slugs (1+). Multiple = treat as a stack."
          },
          "min_share": {
            "type": "number",
            "description": "Peer-share threshold for a gap (0\u20131, default 0.25)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "whats_changed",
      "description": "UNDERSTANDING \u2014 What changed in the catalog since a date: providers added/updated, plus rating movement measured FROM THAT DATE \u2014 each provider's current score against its score at the first scored snapshot on or after `since`, with `was` and `compared_to` on every row. `basis` says whether the answer came from the recorded history or (when no snapshot covers the date) from last-build trend.",
      "inputSchema": {
        "type": "object",
        "required": [
          "since"
        ],
        "properties": {
          "since": {
            "type": "string",
            "description": "YYYY-MM-DD."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "recommend_stack",
      "description": "Design an API stack \u2014 the best-rated catalog provider per capability, assembled into a stack. The Understanding preview is the top pick per capability; the full answer adds alternatives, per-pick artifact gaps, and an exportable APIs.json + Arazzo hint. Decompose the domain into capabilities first, then pass them here.",
      "inputSchema": {
        "type": "object",
        "required": [
          "capabilities"
        ],
        "properties": {
          "capabilities": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The capabilities the stack needs, e.g. [\"payments\",\"email\",\"identity\",\"observability\"]."
          },
          "region": {
            "type": "string",
            "description": "Optional region slug to prefer, e.g. europe."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_apis",
      "description": "Every API one provider publishes, as a page you can filter \u2014 the provider's own catalog rather than the whole network's. Prefer this over find_apis with a `providers` filter when you already know the company: it is the endpoint the provider page itself is built from, so `meta.total` is that provider's real API count. Filter by `tags` (with `match`), require `artifact_types`, or pass include=[\"content\"] to inline artifact bodies.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "artifact_types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Return exactly these top-level keys (overrides view)."
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_artifacts",
      "description": "Every artifact a provider publishes \u2014 provider-level (MCP, security, scopes, rules, agentic-access) and per-API (OpenAPI, Arazzo, overlays, reference docs) \u2014 grouped by type with counts. by_type_counts is the full summary; pass `type` (e.g. OpenAPI, MCP) to scope the artifacts list to one type, since an unfiltered provider can be 250+ entries.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "type": {
            "type": "string",
            "description": "Return only artifacts of this type (synonym-aware: MCP matches MCPServer)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_api_artifacts",
      "description": "One API's artifacts grouped by type (by_type_counts is the full summary); pass `type` to scope the list to one type (synonym-aware: MCP matches MCPServer), include=[\"content\"] to inline the bodies.",
      "inputSchema": {
        "type": "object",
        "required": [
          "aid"
        ],
        "properties": {
          "aid": {
            "type": "string"
          },
          "type": {
            "type": "string",
            "description": "Return only artifacts of this type."
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_openapi",
      "description": "An API's primary OpenAPI reference (url) \u2014 the top agent intent; set include=[\"content\"] to inline the spec body.",
      "inputSchema": {
        "type": "object",
        "required": [
          "aid"
        ],
        "properties": {
          "aid": {
            "type": "string"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_provider_onboarding",
      "description": "A getting-started view for a provider \u2014 website, portal, signup, docs, authentication, base URLs, and first steps assembled from its registered links.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_similar_providers",
      "description": "Providers similar to a given one (alternatives) by shared tags, industry, region, and artifact coverage.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_similar_apis",
      "description": "APIs similar to a given one (\"more like this\") by shared tags.",
      "inputSchema": {
        "type": "object",
        "required": [
          "aid"
        ],
        "properties": {
          "aid": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_industry_leaders",
      "description": "UNDERSTANDING \u2014 Top-rated providers in an industry vertical.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_region_leaders",
      "description": "UNDERSTANDING \u2014 Top-rated providers in a region.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_country_leaders",
      "description": "UNDERSTANDING \u2014 Top-rated providers in one country. Membership is derived and partial, so read the ranking as \"the best of what we have placed there\", not a national league table.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_area_leaders",
      "description": "UNDERSTANDING \u2014 Top-rated providers in a curated API Evangelist area.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_rating_history",
      "description": "UNDERSTANDING \u2014 A provider's REAL score time series: one point per scored build (composite + agent readiness on each date), plus a summary of the movement over the window \u2014 first/last, net change, direction, and the largest single-build jump with the date it happened. Not an implied previous point; these are the recorded snapshots.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_rating_movers",
      "description": "UNDERSTANDING \u2014 The biggest rating movers (up and down) since the previous scoring build.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "story_leads",
      "description": "OWNER \u2014 The weekly \"what's worth writing\" rollup: ranked story leads from the demand map crossed with rating movement \u2014 unmet demand (searched, no result), risers/fallers, and hot-but-thin capabilities. Each lead is a headline, its signal, and a suggested angle. include=[\"raw\"] appends the underlying demand report.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "raw"
              ]
            }
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "what_can_i_fix",
      "description": "INFLUENCE \u2014 the ranked, costed, gated punch list for one provider you own. Every agent-readiness check with points still on the table (exact \u2014 these are real per-check results, each with what satisfies it), plus the Kin Score facets ranked by weight x headroom rather than by lowest score. `do_first` is the headline and prefers a BAND GATE over any amount of points, because points cannot clear a gate. Use readiness_gates for the gate detail and simulate_fixes to price a plan before committing to it.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "readiness_gates",
      "description": "INFLUENCE \u2014 what blocks the next agent-readiness band, GATES BEFORE POINTS. Some bands are unreachable on score alone: agent-native requires idempotency AND a stable error envelope regardless of how many points a provider collects. Returns the points gap and the unmet gate requirements separately, each priced, plus a verdict saying whether clearing the gate also closes the gap \u2014 the case a points-arithmetic answer gets wrong every time.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "simulate_fixes",
      "description": "INFLUENCE \u2014 project a set of fixes onto a provider's agent-readiness score and band before doing the work. Re-applies the band gate to the PROJECTED state, so it will tell you when buying points still leaves you demoted. Kin Score facets are refused rather than estimated: several checks behind them are count-based across a provider's APIs, so a single claimed fix has no computable composite value.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug",
          "fixes"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "fixes": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Agent-readiness dimension ids to assume fixed, e.g. [\"idempotency\",\"mcp_server\"]. what_can_i_fix lists the available ones."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "claim_listing",
      "description": "INFLUENCE \u2014 ask to claim a listing as yours. Returns 202 with a token and the hosts it will be checked against: publish it as a DNS TXT record, or as a file at /.well-known/apis-io-challenge, and leave it up. A PERSON verifies it and emails you \u2014 there is nothing else to call. Track it with check_status.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "generate_artifact",
      "description": "INFLUENCE \u2014 REQUEST an artifact you are being marked down for not having: apis-json, agent-card, mcp (one tool per operation you already publish), arazzo, rules (a Spectral ruleset scoped to your own failing checks), scopes and security (read out of your own OpenAPI). Returns 202 \u2014 a person generates it against your live surface, checks it, and sends it to you. Nothing is published on your behalf either way: your score moves when YOU commit the file. Omit `artifact` to list what can be made.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "artifact": {
            "type": "string",
            "enum": [
              "apis-json",
              "agent-card",
              "mcp",
              "arazzo",
              "rules",
              "scopes",
              "security"
            ]
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "submit_artifact",
      "description": "INFLUENCE \u2014 tell us about an artifact you publish, rather than waiting for us to find it. Returns 202; a person fetches it, checks it and re-runs the pipeline against it. Track it with check_status.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug",
          "type",
          "url"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "type": {
            "type": "string",
            "description": "e.g. OpenAPI, AsyncAPI, MCP."
          },
          "url": {
            "type": "string"
          },
          "contact": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "correct_facts",
      "description": "INFLUENCE \u2014 propose corrections to what we say about you: name, description, url, industries, tags. Returns 202 and is applied by a person \u2014 an API that let an owner rewrite their own description unreviewed would be a marketing surface, not a catalog. Score and band are not owner-settable and are ignored.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "url": {
            "type": "string"
          },
          "industries": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "contact": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "dispute_finding",
      "description": "INFLUENCE \u2014 \"you say we lack X, here it is.\" Send the claim in words and a URL that evidences it. Returns 202; a person fetches your evidence, checks it against what we hold, and emails you either way. Open to any paying caller \u2014 you should not have to prove ownership before you can tell us our data is wrong.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug",
          "claim"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "claim": {
            "type": "string"
          },
          "evidence_url": {
            "type": "string"
          },
          "contact": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "set_visibility",
      "description": "INFLUENCE \u2014 ask to be shown less, or not at all. `restricted` keeps a name, a description and a link to your own site, unrated and out of every ranked view; `delisted` removes you entirely. Returns 202 and is applied by a person: it strips artifacts, pages and rollups across the network, which is not something to do irreversibly on one API call.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug",
          "visibility"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "visibility": {
            "type": "string",
            "enum": [
              "restricted",
              "delisted"
            ]
          },
          "reason": {
            "type": "string"
          },
          "contact": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "watch_listing",
      "description": "INFLUENCE \u2014 be told when a listing you care about moves: score changes, band changes, or a check closing. Registration only \u2014 the scoring pipeline sends the mail, this records what you want to hear about. Re-registering the same slug REPLACES its event set rather than adding a second watch. list_watches shows what you hold; unwatch_listing removes one.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "events": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "score, band, check. Defaults to all three."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "list_watches",
      "description": "INFLUENCE \u2014 the listings you are watching, with the events registered for each and when you started. The read half of watch_listing: call it before registering another, so you neither duplicate a watch nor assume one is in place that never took.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "unwatch_listing",
      "description": "INFLUENCE \u2014 stop watching a listing. Takes effect immediately and removes the registration outright; there is no paused state. Removing a watch that was never registered is not an error.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "request_check",
      "description": "INFLUENCE \u2014 ask APIs.io to (re)check something. Use it when a listing is wrong or stale, when a score looks incorrect, or to get a company profiled that is not in the catalog yet. A check can target a PROVIDER (the default) or a slice of the catalog: an `industry`, a `tag`, an `area`, or a company's `estate` \u2014 for an industry roster missing an obvious member, a tag that has drifted, or an area whose ranking looks wrong. This does NOT score anything on the spot: a check means re-running the enrichment pipeline against the live surface, which a person does by hand. You get a request id back immediately; poll check_status with it, and if you supply a contact email you are notified when the status changes. A slug we do not already hold is never refused \u2014 for ANY target type it is queued as a request to ADD that thing, whether that is a company nobody has catalogued, a new industry, or an estate nobody here has thought of. Near matches come back alongside it in `did_you_mean`, so a typo is visible without the request being dropped.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "target_type": {
            "type": "string",
            "enum": [
              "provider",
              "industry",
              "tag",
              "area",
              "estate"
            ],
            "description": "What `slug` names. Defaults to provider. `estate` is named for the company the estate roots at (e.g. cisco)."
          },
          "slug": {
            "type": "string",
            "description": "The target slug \u2014 a provider (e.g. stripe), or an industry/tag/area/estate slug when target_type says so."
          },
          "url": {
            "type": "string",
            "description": "The company or developer-portal URL \u2014 use this when there is no listing yet. Providers only; a url cannot name an industry."
          },
          "kind": {
            "type": "string",
            "enum": [
              "recheck",
              "profile",
              "correction"
            ],
            "description": "recheck an existing listing, profile a new company, or correct a specific fact."
          },
          "contact": {
            "type": "string",
            "description": "Email to notify when the status changes. Optional; without it there is nothing to notify, so keep the id."
          },
          "notes": {
            "type": "string",
            "description": "What is wrong, or what to look at. The more specific, the faster it is worked."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "check_status",
      "description": "INFLUENCE \u2014 the status of a check request by id: queued, in_review, running, done, rejected or needs_info, with any notes shared by the reviewer. The id identifies the request, but the Influence plan is now required to read it back. To report an error for free, use report_correction or open an issue on the provider's api-evangelist repo.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "report_gap",
      "description": "FREE, no account \u2014 tell the catalog what you looked for and could not find. The consumer door (roadmap#166): a gap report is how the catalog learns what to add next, and a person reads the queue. One required field.",
      "inputSchema": {
        "type": "object",
        "required": [
          "looked_for"
        ],
        "properties": {
          "looked_for": {
            "type": "string",
            "description": "What you were looking for, in your own words."
          },
          "needed_for": {
            "type": "string",
            "description": "Optional: the task you needed it for."
          },
          "found": {
            "type": "string",
            "enum": [
              "nothing",
              "wrong thing",
              "listed but unusable"
            ]
          },
          "provider": {
            "type": "string",
            "description": "Optional slug, when the gap is about an existing listing."
          },
          "contact": {
            "type": "string",
            "description": "Optional email if you want to hear when the catalog can answer this."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "my_checks",
      "description": "INFLUENCE \u2014 the check requests you filed, newest first. Requires a key or login \u2014 an anonymous caller has no identity to scope a list to, and should read a single request with check_status instead.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "export_dataset",
      "description": "LICENCE (sold on the Influence plan) \u2014 the WHOLE dataset, not a 100-row page: `ratings` (every rated provider with facets, trend, rubric + provenance), `providers` (the catalog with coverage + score), or `history` (every provider's full score time series \u2014 parallel date/composite/agent_readiness arrays across every scored build the catalog retains, with a movement summary per provider). `history` is the one nobody can reconstruct after the fact: it is the only record of what the catalog looked like on those dates. For media outlets and analysts licensing the data to publish rankings and trends. Every dataset exceeds one response, so they arrive in byte-budgeted chunks: while `complete` is false, call again with `cursor` set to `next_cursor` and concatenate `data`; the last page has `complete: true` and a null `next_cursor`. Attribute to apis.io and cite the rubric.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dataset": {
            "type": "string",
            "enum": [
              "ratings",
              "providers",
              "history"
            ],
            "default": "ratings"
          },
          "cursor": {
            "type": "integer",
            "minimum": 0,
            "description": "Resume offset from a previous response's next_cursor. Omit for the first page; keep calling while next_cursor is not null."
          },
          "co_brand": {
            "type": "string",
            "description": "Optional partner name for a co-branded license \u2014 stamps the attribution the license carries."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "industry_gap_analysis",
      "description": "UNDERSTANDING \u2014 Valuable artifact types (MCP, Arazzo, Rules, Skills\u2026) commonly missing across a whole industry \u2014 where the vertical is under-served.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "min_share": {
            "type": "number",
            "description": "Coverage threshold below which a type counts as an industry gap (0\u20131, default 0.5)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "export_stack",
      "description": "UNDERSTANDING \u2014 Export a designed capability stack as an adoptable APIs.json document (with an Arazzo hint) the team can commit.",
      "inputSchema": {
        "type": "object",
        "required": [
          "capabilities"
        ],
        "properties": {
          "capabilities": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The capabilities the stack needs, e.g. [\"payments\",\"email\",\"identity\"]."
          },
          "region": {
            "type": "string",
            "description": "Optional region slug to prefer."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "insights_overview",
      "description": "UNDERSTANDING \u2014 the Insights root: how many companies are profiled, over how many quarters, and the top investment dimensions across the whole set. Read this FIRST \u2014 it tells you the size and vintage of the corpus before you draw a conclusion from any single company in it.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_company_insights",
      "description": "Browse profiled companies (Fortune 1000) by name or industry; ranked by overall technology-readiness signal. `industry` takes an insights industry NAME from insights_industries (e.g. \"Industrial\") \u2014 a different vocabulary from the catalog industry slugs find_providers uses \u2014 and matches every roster a company sits on, not just its primary. Understanding plan.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string"
          },
          "industry": {
            "type": "string"
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "insights_dimensions",
      "description": "Cross-section ranking of the 40 tracked investment dimensions (AI, cloud, governance, security, \u2026) by how many profiled companies show signal. Understanding plan.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "insights_adoption",
      "description": "Services, tools, or standards ranked by how many companies adopt them across the profiled set. Understanding plan.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "service",
              "tool",
              "standard"
            ],
            "default": "service"
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "insights_industries",
      "description": "UNDERSTANDING \u2014 the demand-side industry rollup: the sectors the profiled companies fall into. Each row carries TWO counts and they answer different questions: `company_count` is the research roster, `profiled_count` is how many of those you can actually read back via find_company_insights(industry:) \u2014 size a cohort on profiled_count. The counterpart to find_industries, which counts SUPPLY (providers publishing APIs into a vertical) \u2014 this counts DEMAND (companies buying into it). Comparing the two is how you find a sector with buyers and no sellers.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_company_insight",
      "description": "UNDERSTANDING \u2014 Full demand-side profile for one company: 40-dimension readiness scores, adopted stack, and per-quarter history.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "company_gaps",
      "description": "UNDERSTANDING \u2014 A company's weakest investment dimensions \u2014 where it is thin and a vendor could sell in.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "match_providers",
      "description": "UNDERSTANDING \u2014 The supply\u2194demand join: apis.io providers whose product appears in a company's adopted stack, ranked by rating.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_vcs",
      "description": "UNDERSTANDING \u2014 Find venture-capital firms by name, thesis tags, or category. Each carries a network-matched, rated portfolio graph. Sort by portfolio_on_network (default), portfolio_total, portfolio_rating, or name. Understanding plan.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "category": {
            "type": "string",
            "description": "Firm category, e.g. \"Venture Capital\"."
          },
          "sort": {
            "type": "string",
            "enum": [
              "portfolio_on_network",
              "portfolio_total",
              "portfolio_rating",
              "name"
            ]
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_vc",
      "description": "UNDERSTANDING \u2014 One VC firm: identity, fund facts, and portfolio summary (totals, band distribution, average rating). Pass view=full to inline the whole portfolio. Understanding plan.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "view": {
            "type": "string",
            "enum": [
              "summary",
              "full"
            ],
            "default": "summary",
            "description": "summary (default) returns lean discovery records + *_count for dropped sections; full returns the whole record (use get_api / get_provider for one entity)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "vc_portfolio",
      "description": "UNDERSTANDING \u2014 A VC firm's portfolio companies, each matched to the network and rated. Filter publishes_api=true (companies that actually ship an API \u2014 about 37% of any book), in_network=true, band, or min_score; sort by score (default), api_count, or name. The demand-side view of who a fund has backed. Understanding plan.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "publishes_api": {
            "type": "boolean",
            "description": "Only portfolio companies that publish an API indexed on the network \u2014 the real rate is about 37% of a book, not 100%. This is the filter you want for \"how much of this fund ships an API\"."
          },
          "is_provider": {
            "type": "boolean",
            "description": "Only portfolio companies with a provider profile on the network. Nearly every row qualifies (a profile is created for each portfolio company), so this barely narrows anything \u2014 use publishes_api instead."
          },
          "in_network": {
            "type": "boolean"
          },
          "band": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Rating bands: exemplar, strong, developing, thin, minimal."
          },
          "min_score": {
            "type": "number"
          },
          "sort": {
            "type": "string",
            "enum": [
              "score",
              "api_count",
              "name"
            ]
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_investors",
      "description": "UNDERSTANDING \u2014 The reverse portfolio edge: which VC firms in the network back a given provider/company (by slug). The investor COUNT is free; the named firms need Understanding.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug"
        ],
        "properties": {
          "slug": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "resolve",
      "description": "Resolve ANY identifier \u2014 website URL, domain (stripe.com), or GitHub org (github.com/stripe) \u2014 to the apis.io provider it belongs to. Use this when you have a URL, not a slug. Understanding plan.",
      "inputSchema": {
        "type": "object",
        "required": [
          "identifier"
        ],
        "properties": {
          "identifier": {
            "type": "string",
            "description": "A domain, URL, or github.com/<org>."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "enrich_provider",
      "description": "One call, pick your field groups \u2014 resolves a slug OR any identifier and returns exactly the groups you ask for, instead of chaining get_provider + get_provider_rating + get_provider_artifacts + get_provider_onboarding. Groups: profile, onboarding, artifacts, rating, insights. Understanding plan \u2014 the base groups moved with the rest of the discovery layer on 2026-08-31.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "Provider slug OR any identifier resolve() accepts."
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Field groups: profile, onboarding, artifacts, rating, security, insights. Default: profile+onboarding+artifacts+rating."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "save_search",
      "description": "UNDERSTANDING \u2014 Persist the current search criteria as a named saved search you own, so you can re-run it and get net-new results later. Criteria use the same filters as apis_io_search. Requires an apis.io API key.",
      "inputSchema": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "band": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Rating bands: exemplar, strong, developing, thin, minimal."
          },
          "industry": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "region": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "area": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "artifact_types": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "min_score": {
            "type": "number"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "my_workspace",
      "description": "UNDERSTANDING \u2014 the workspace root: which plan you are on, how many saved searches and lists you hold, and where each lives. Answers \"what do I have here and what can I afford\" in one call, so an agent need not probe. NOT the same as the account (login, API key, usage, billing), which is https://apis.io/api/v1/auth/me.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "list_saved_searches",
      "description": "UNDERSTANDING \u2014 List the saved searches you own (name, criteria, when last run, how many matches are tracked).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "run_saved_search",
      "description": "UNDERSTANDING \u2014 Re-run one of your saved searches against the live catalog and return current matching providers.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "saved_search_net_new",
      "description": "UNDERSTANDING \u2014 What is NEW for a saved search since you last checked: providers that newly match your criteria. Advances the delta cursor, so each call reports only what changed since the previous call. The agent-native version of the demand report.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "delete_saved_search",
      "description": "UNDERSTANDING \u2014 Delete a saved search you own.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "create_list",
      "description": "UNDERSTANDING \u2014 Create a named, persistent list (a shortlist / stack you own) of providers or APIs. Requires an apis.io API key.",
      "inputSchema": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "members": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Provider slugs and/or aids to seed the list."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "list_lists",
      "description": "UNDERSTANDING \u2014 List the lists/shortlists you own.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "get_list",
      "description": "UNDERSTANDING \u2014 Get a list you own, with each member resolved to its current name, band, and score.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "add_to_list",
      "description": "UNDERSTANDING \u2014 Add providers/APIs (by slug or aid) to one of your lists.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id",
          "members"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "members": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "delete_list",
      "description": "UNDERSTANDING \u2014 Delete a list you own.",
      "inputSchema": {
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_openapis",
      "description": "OpenAPI specifications across the catalog. The REST contract. get_openapi returns one API's primary spec with optional inlined body. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_asyncapis",
      "description": "AsyncAPI (event-driven) specifications across the catalog. Event/streaming APIs. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "version": {
            "type": "string",
            "description": "AsyncAPI spec version \u2014 the `asyncapi:` declaration, e.g. 2.6.0 or 3.0.0."
          },
          "info_version": {
            "type": "string",
            "description": "The document's OWN version from info.version, set by whoever wrote the spec. Absent on 70% of them."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_channels",
      "description": "AsyncAPI event channels across the catalog. Individual event channels (topics/streams). Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "protocol": {
            "type": "string",
            "description": "Transport the channel runs over: ws, http, sse, kafka, mqtt, amqp, nats, grpc\u2026 Empty where the document never says, which is most of them."
          },
          "version": {
            "type": "string",
            "description": "AsyncAPI spec version the channel is defined in, e.g. 2.6.0 or 3.0.0."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_graphql",
      "description": "GraphQL schemas across the catalog. GraphQL type systems. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_arazzo",
      "description": "Arazzo workflows across the catalog. Multi-step API workflows chaining operations. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_mcp",
      "description": "MCP servers across the catalog. Model Context Protocol servers a provider offers to agents. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "install_method": {
            "type": "string",
            "description": "How the server is delivered: \"Hosted endpoint\", \"GitHub repo\", \"Documentation link\"."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_skills",
      "description": "Agent Skills across the catalog. Packaged agent skills. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_rules",
      "description": "Spectral governance rulesets across the catalog. API design/linting rulesets. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "rule_count"
            ],
            "description": "Order: relevance / provider / name, or rule_count (largest first)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_scopes",
      "description": "OAuth scope catalogs across the catalog. The OAuth scopes an API defines. Understanding plan. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "scope_count"
            ],
            "description": "Order: relevance / provider / name, or scope_count (largest first)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_security",
      "description": "Security artifacts across the catalog. Authentication, domain security, VDP, trust center. Understanding plan. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "scheme_count"
            ],
            "description": "Order: relevance / provider / name, or scheme_count (largest first)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_plans",
      "description": "Pricing plans across the catalog. An API's pricing tiers. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "plan_count"
            ],
            "description": "Order: relevance / provider / name, or plan_count (largest first)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_rate_limits",
      "description": "Rate-limit policies across the catalog. Declared throttling policies. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "limit_count"
            ],
            "description": "Order: relevance / provider / name, or limit_count (largest first)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_finops",
      "description": "FinOps / billing models across the catalog. How usage is metered and billed. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "billing_model": {
            "type": "string",
            "description": "Billing model, e.g. subscription, consumption, prepaid."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_collections",
      "description": "Postman / Open collections across the catalog. Runnable request collections. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "item_count"
            ],
            "description": "Order: relevance / provider / name, or item_count (largest first)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_json_ld",
      "description": "JSON-LD contexts across the catalog. Linked-data vocabularies. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "class_count",
              "property_count"
            ],
            "description": "Order: relevance / provider / name, or class_count / property_count (largest first)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_json_schemas",
      "description": "JSON Schemas across the catalog. Standalone data schemas. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_json_structures",
      "description": "JSON Structures across the catalog. Data structures behind an API. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "property_count"
            ],
            "description": "Order: relevance / provider / name, or property_count (largest first)."
          },
          "struct_type": {
            "type": "string",
            "description": "Root type: object, array, datetime, \u2026"
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_examples",
      "description": "Example payloads across the catalog. Real request/response examples. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name",
              "key_count"
            ],
            "description": "Order: relevance / provider / name, or key_count (largest first)."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_postman",
      "description": "Postman collections across the catalog. Postman-format collections (by reference). Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "find_apis_json",
      "description": "APIs.json indexes across the catalog. The APIs.json discovery documents themselves. Filter by q / tags / providers; include=[\"content\"] inlines bodies. Use find_artifacts for cross-type search.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": {
            "type": "string",
            "description": "Free text over name + description."
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Tag slugs. On PROVIDERS this is identity \u2014 what the company says it does, from its own apis.yml. A resource or operation term (webhook, saml, order, oai-pmh) will correctly match no provider; use find_apis?tags= for those, or api_tags below."
          },
          "providers": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "match": {
            "type": "string",
            "enum": [
              "any",
              "all"
            ],
            "default": "any"
          },
          "include": {
            "type": "array",
            "items": {
              "enum": [
                "content"
              ]
            }
          },
          "page": {
            "type": "integer",
            "minimum": 1,
            "default": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25
          },
          "sort": {
            "type": "string",
            "enum": [
              "relevance",
              "provider",
              "name"
            ],
            "description": "Order: relevance (catalog order), provider, or name."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "submit_feedback",
      "description": "Report something wrong with a result: wrong data, a filter that did not filter, a ranking that put off-topic results first, a number you could not reproduce, or an answer that was useless for your task. Free, and the fastest way to get a defect fixed \u2014 this is read by a human. Say what you expected as well as what you got.",
      "inputSchema": {
        "type": "object",
        "required": [
          "problem"
        ],
        "properties": {
          "problem": {
            "type": "string",
            "description": "What was wrong. Be specific \u2014 the tool you called, the arguments, and what came back."
          },
          "expected": {
            "type": "string",
            "description": "What you expected instead."
          },
          "about": {
            "type": "string",
            "description": "Optional slug, aid or tag the report concerns."
          },
          "tool": {
            "type": "string",
            "description": "Optional name of the tool that produced it."
          },
          "severity": {
            "type": "string",
            "enum": [
              "wrong",
              "misleading",
              "unhelpful"
            ],
            "description": "wrong = factually incorrect; misleading = correct but reads as something else; unhelpful = right and useless."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    },
    {
      "name": "report_correction",
      "description": "Report that the catalog has a provider WRONG \u2014 a wrong website, a wrong logo, APIs that are not theirs, a count that does not match what they ship, a score computed from any of those. Free, always: correcting our own error is never a paid feature. Read by a human. If you are the provider, say so \u2014 it does not gate anything, it tells us how fast to move.",
      "inputSchema": {
        "type": "object",
        "required": [
          "slug",
          "wrong"
        ],
        "properties": {
          "slug": {
            "type": "string",
            "description": "The provider slug the record is filed under."
          },
          "wrong": {
            "type": "string",
            "description": "What is incorrect. Name the field if you can."
          },
          "correct": {
            "type": "string",
            "description": "What it should say instead."
          },
          "evidence": {
            "type": "string",
            "description": "A URL that shows it \u2014 your own docs, your own site. This is what makes a correction actionable rather than a claim."
          },
          "field": {
            "type": "string",
            "description": "Optional field name: website, image, api_count, tags, score, access_model, apis."
          },
          "relationship": {
            "type": "string",
            "enum": [
              "provider",
              "customer",
              "observer"
            ],
            "description": "Your relationship to the provider. Never gates the report; it sets priority."
          },
          "context": {
            "type": "string",
            "description": "Optional: why you are asking. One sentence \u2014 the task you are trying to complete, or what you expect to get back. Never included in the answer and never used to rank; it is read only when a result turns out to be wrong, which is when knowing the intent is what makes the report actionable."
          }
        }
      }
    }
  ]
}